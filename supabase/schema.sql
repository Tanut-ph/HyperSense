-- ============================================================================
-- HyperSense — Supabase Schema
-- ----------------------------------------------------------------------------
-- สคีมาจริงของระบบอยู่ที่ไฟล์เดียวที่รันได้ทันที:
--
--        ▶  database/reset_and_seed.sql
--
-- เปิด Supabase SQL Editor แล้ววางไฟล์ reset_and_seed.sql ทั้งไฟล์
-- (ลบของเก่า + สร้างตารางใหม่ + ใส่ข้อมูลตัวอย่าง ในครั้งเดียว)
-- ============================================================================
