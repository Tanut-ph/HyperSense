-- ============================================================================
-- HyperSense — Database Schema
-- ----------------------------------------------------------------------------
-- โครงสร้างฐานข้อมูลถูกรวมไว้ในไฟล์เดียวที่รันได้ทันที (drop เก่า + สร้างใหม่ + ใส่ข้อมูล)
--
--        ▶  database/reset_and_seed.sql
--
-- เปิด Supabase SQL Editor แล้ววางไฟล์ reset_and_seed.sql ทั้งไฟล์เพื่อ:
--   1) ลบตาราง/ข้อมูลเก่าทั้งหมด
--   2) สร้างตารางใหม่ (staff, patients, patient_history, patient_comorbidities,
--      patient_medications, bp_visits, patient_labs, doctor_records)
--   3) ใส่ข้อมูลตัวอย่าง (ผู้ป่วย 9 คน + บัญชีพนักงาน 5 คน)
-- ============================================================================
